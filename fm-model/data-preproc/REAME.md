Code comes from: https://github.com/ivadomed/SpineFoundation

The current folder is used to:
- download dataset automatically
- split data into train, val and test
- creates a files.json file with the splits

This is all done by running: `./download_data.sh`